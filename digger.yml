projects:
  - name: iac-orchestration-example
    dir: .
    workspace: default
    terragrunt: false
    workflow: default

workflows:
  default:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - init
        - apply
